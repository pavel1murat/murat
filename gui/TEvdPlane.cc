///////////////////////////////////////////////////////////////////////////////

#include "murat/gui/TEvdPlane.hh"

ClassImp(murat::TEvdPlane)

namespace murat {
//-----------------------------------------------------------------------------
TEvdPlane::TEvdPlane(int I): TEvdSubdetector() {
  // fNumber = I;
  // for (int i=0; i<kNPanels; i++) {
  //   fPanel[i]         = new TEvdPanel(i);
  //   fPanel[i]->fPlane = this;
  //   AddElement(fPanel[i]);
  // }
  // SetRnrSelfChildren(false,true);
}  

}
